[package]
name = "pow_blockchain"
version = "0.1.0"
edition = "2021"

[lib]
name = "pow_blockchain"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rust-crypto = "0.2"
num-bigint = "0.4"
num-traits = "0.2"
lazy_static = "1.4"
